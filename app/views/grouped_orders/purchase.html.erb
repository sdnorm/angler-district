<div class="row">

  <p>
    <strong>Address1:</strong>
    <%= @grouped_order.address1 %>
  </p>

  <p>
    <strong>City:</strong>
    <%= @grouped_order.city %>
  </p>

  <p>
    <strong>State:</strong>
    <%= @grouped_order.state %>
  </p>

  <p>
    <strong>Total:</strong>
    $<%= Money.new(@grouped_order.total*100, "USD") %>
  </p>

  <%= form_tag charges_path(order: @grouped_order.id) do %>
    <article>
      <% if flash[:error].present? %>
        <div id="error_explanation">
          <p><%= flash[:error] %></p>
        </div>
      <% end %>
      <label class="amount">
        <span>$<%= Money.new(@grouped_order.total*100, "USD") %></span>
      </label>
    </article>
    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
            data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
            data-description="Tackle!"
            data-amount=<%= @grouped_order.total*100 %>
            data-locale="auto"></script>
  <% end %>

  <%= link_to 'Edit', edit_grouped_order_path(@grouped_order) %>
</div>
